// import mongoose from "mongoose";

// const url = process.env.MONGODB_URI || `mongodb://localhost:27017/bug-tracker`;
// console.log("Connection established succesfully at", url);
// // mongoose.Promise = global.Promise;
// mongoose.set("useNewUrlParser", true);
// mongoose.set("useFindAndModify", false);
// mongoose.set("useCreateIndex", true);
// mongoose.set("useUnifiedTopology", true);

// module.exports = mongoose.connect(url, () => console.log("Standing By..."));

// Obsolete - code shifted to index.js
